# FakeNewsAI Python requirements

# --- Web framework ---------------------------------------------------------
Flask>=2.3.0
# Per-IP rate limiting for the expensive endpoints (/api/analyze, etc.).
Flask-Limiter>=3.5.0
# Production-grade threaded WSGI server, so one slow request cannot block
# every other client the way the single-threaded dev server did.
waitress>=3.0.0

# --- Machine learning & data ----------------------------------------------
scikit-learn>=1.3.0
pandas>=1.5.0
numpy>=1.24.0
joblib>=1.3.0

# --- HTTP & configuration --------------------------------------------------
requests>=2.31.0
python-dotenv>=1.0.0
# Public Google Drive dataset-folder download helper.
gdown>=5.2.0

# --- Optional: local LLM synthesis (only needed when ENABLE_LLM=true) ------
# Install with: pip install -r requirements-llm.txt
# transformers>=4.40.0
# torch>=2.0.0
# accelerate>=0.25.0
# huggingface-hub>=0.20.0

# --- Testing ---------------------------------------------------------------
pytest>=7.4.0
